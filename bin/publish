#!/usr/bin/env sh
source "${0%/*}/utils.sh"

[ -e "$1" ] || die "usage: publish <path-to-draft>"

draft="$1"
post="_posts/$(date +%Y-%m-%d)-${draft##*/}"

[ -e "$post" ] && die "$post exists."

mv -v "$draft" "$post"
