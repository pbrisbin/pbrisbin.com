#!/usr/bin/env sh
set -e

tag="v$(date +%Y%m%d%H%M)"
host='pbrisbin.com'

jekyll build

rsync -e ssh --archive _site/ "$host:/srv/http/site/"

git tag -a -m "$tag" "$tag"

printf "success: %s -> %s\n" "$tag" "$host"
printf "pushing to github...\n"

git push
git push --tags
